## Workflow

This project uses the Get Shit Done (GSD) workflow for planning and execution.

All workflow management is handled by GSD agents. Planning documents and execution state are maintained in the `.planning/` directory.
